{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "stats": "set NODE_ENV=production && webpack --profile --json > compilation-stats.json",
    "build": "set NODE_ENV=production && npm run clean && webpack --progress",
    "dev": "set NODE_ENV=development && npm run clean && webpack-dev-server --port 9000 --hot --inline --progress"
  },
  "author": "Kawin Glomjai",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^16.3.16",
    "@types/react-dom": "^16.0.6",
    "@types/react-loadable": "^5.4.0",
    "@types/react-router": "^4.0.26",
    "@types/react-router-dom": "^4.2.7"
  },
  "dependencies": {
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-loadable": "^5.4.0",
    "react-router": "^4.2.0",
    "react-router-dom": "^4.2.2"
  }
}
