{
  "private": true,
  "name": "webpack-stats",
  "version": "1.0.0",
  "description": "Webpack 4 had has a `prefetch` while dynamic import. we try to use the stat object file to generate HTML link tags.",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gkawin/prefetch-tag.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gkawin/prefetch-tag/issues"
  },
  "homepage": "https://github.com/gkawin/prefetch-tag#readme",
  "dependencies": {
    "lerna": "^2.11.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}
